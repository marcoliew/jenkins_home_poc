pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Hello from Git!'
                sh 'ls -l'  // Optional: List files to verify workspace
            }
        }
    }
}